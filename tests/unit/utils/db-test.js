import db from 'reading-list/utils/db';
import { module, test } from 'qunit';

module('Unit | Utility | db', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = db();
    assert.ok(result);
  });
});
